# Local env file with the admin password. Never commit.
.env
.env.*
!.env.example

# Stale Python state from the v0.1 Streamlit shape — kept ignored
# so an old `.venv/` lying around doesn't accidentally land in a
# commit. The curator no longer uses Python at all (CLAUDE.md +
# bash scripts only); these patterns can be deleted in the next
# clean-up pass.
.venv/
venv/
env/
__pycache__/
*.py[cod]

# IDEs
.idea/
.vscode/
*.swp
.DS_Store
