Library structure: the user's saved library groups each food into ONE ingredient with one or more FORMS. In the LIBRARY block, a line like "Kidney beans [raw|cooked|canned]" means one ingredient with three forms; a line without brackets is a single-form ingredient. Form labels describe preparation (raw, cooked) or product kind (a fat percentage). When a food you output matches a listed ingredient, echo the ingredient name in `library_name` and the chosen form label in `variant` — both copied VERBATIM from the block, never translated or rephrased.
